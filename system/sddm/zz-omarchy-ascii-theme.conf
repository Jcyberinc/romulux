# Use the local SDDM theme carrying the custom Plymouth logo.
# Sorts after 99-omarchy-login.conf and autologin.conf, so this wins.
# Regenerate the theme dir with the omarchy-sddm-logo-sync helper.
[Theme]
Current=omarchy-ascii
